@model List<BookStore_Client.Models.Supplier>

@{
    ViewData["Title"] = "Supplier List";
    Layout = "~/Views/Shared/_layoutadmin.cshtml";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-lg-12">
            <div class="card shadow-lg border-0">
                <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center">
                    <h4 class="mb-0">Supplier List</h4>
                    <a asp-action="Create" class="btn btn-success btn-sm">Add New Supplier</a>
                </div>
                <div class="card-body p-4">
                    <div class="table-responsive">
                        <table class="table table-hover table-striped align-middle">
                            <thead class="table-dark">
                                <tr>
                                    <th scope="col">Supplier Name</th>
                                    <th scope="col">Phone</th>
                                    <th scope="col">Email</th>
                                    <th scope="col" class="text-center">Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                @if (Model != null && Model.Any())
                                {
                                    @foreach (var supplier in Model)
                                    {
                                        <tr>
                                            <td>@supplier.SupplierName</td>
                                            <td>@supplier.Phone</td>
                                            <td>@supplier.Email</td>
                                            <td class="text-center">
                                                <a asp-action="Details" asp-route-id="@supplier.SupplierID" class="btn btn-info btn-sm me-1">View</a>
                                                <a asp-action="Edit" asp-route-id="@supplier.SupplierID" class="btn btn-warning btn-sm me-1">Edit</a>
                                                <a asp-action="Delete" asp-route-id="@supplier.SupplierID" class="btn btn-danger btn-sm">Delete</a>
                                            </td>
                                        </tr>
                                    }
                                }
                                else
                                {
                                    <tr>
                                        <td colspan="4" class="text-center text-muted py-4">No suppliers found.</td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>